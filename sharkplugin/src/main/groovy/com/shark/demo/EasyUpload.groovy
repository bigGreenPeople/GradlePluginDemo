package com.shark.demo

import org.gradle.api.Project
import org.gradle.api.Plugin

class EasyUpload implements Plugin<Project> {

    @Override
    void apply(Project project) {
        // 构建逻辑
        println "Hello.fu jie..........................................................................."
    }
}
